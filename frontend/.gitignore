# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
!.env.local.example

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# tauri
src-tauri/target/

# tauri android — gen/android/ itself is generated by `tauri android init` and
# ships its own .gitignore for build/.gradle caches (already correct: only
# source, manifests, and the Gradle wrapper are tracked). These rules are
# defense-in-depth in case that nested .gitignore is ever missing/regenerated
# before the caches below exist.
src-tauri/gen/android/.gradle/
src-tauri/gen/android/build/
src-tauri/gen/android/*/build/
src-tauri/gen/android/*/*/build/
src-tauri/gen/android/local.properties
src-tauri/gen/android/*.iml
src-tauri/gen/android/.idea/

# Android release signing material — CI writes these into app/ from GitHub
# Secrets at build time and deletes them afterward. Must never be committed:
# the .jks is the private key that signs every release.
src-tauri/gen/android/app/*.jks
src-tauri/gen/android/app/*.keystore
src-tauri/gen/android/app/key.properties
src-tauri/gen/android/app/keystore.properties
